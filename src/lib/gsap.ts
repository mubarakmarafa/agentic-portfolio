/**
 * GSAP setup placeholder for Phase 3 animations.
 *
 * When ready:
 *   npm install gsap @gsap/react
 *   import { gsap } from "gsap";
 *   import { ScrollTrigger } from "gsap/ScrollTrigger";
 *   gsap.registerPlugin(ScrollTrigger);
 *
 * Use useGSAP from @gsap/react with scope refs for cleanup.
 */

export const GSAP_PLACEHOLDER = true;
