/**
 * Secret message reveal is rendered inline in Footer when "secret-message" is unlocked.
 * This component exists as a registry hook for future standalone reveal effects.
 */
export function SecretReveal() {
  return null;
}
