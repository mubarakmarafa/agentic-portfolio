/**
 * Drop zones are hit-tested in JS via secretZones.ts + DOM refs on Hero/Footer.
 * This module documents the approach; no visible drop zone UI is rendered.
 */
export const DROP_ZONE_HIT_TEST = "pointer-events-none";
